[build]
  command = "pip install -r requirements_web.txt"
  publish = "."

[[redirects]]
  from = "/*"
  to = "/app.py"
  status = 200

[dev]
  command = "streamlit run app.py --server.port 8501 --server.address 0.0.0.0"
  port = 8501
  publish = 8501
